# Public work policy

Fesnyng is public. Treat every GitHub surface as public, including issue and pull-request text, comments, commits, pushes, branches and tags, release metadata, CI output and logs, screenshots, attachments, uploaded artifacts, and linked evidence.

Before committing, and before every GitHub write, upload, or edit, review the complete outgoing source and relevant history, metadata, branch or tag names, commit message and identity, links, attachments, and generated output. Publish only generic or fictional examples and sanitized evidence. Never publish credentials, tokens, keys, private URLs, personal contact details, private organization/customer/employer identifiers, host or machine inventories, raw session/tool transcripts, or raw private evidence. Keep any required unsanitized original outside this checkout in an owner-approved private destination; if none exists, resolve that destination before retaining or sharing it.

Use the approved no-reply commit email `259149591+Sterling-Automation@users.noreply.github.com`; GitHub may display the approved account's name after the authenticated author login `Sterling-Automation` is verified. Verify GitHub-created merge metadata after delivery. Preserve intentional public attribution, canonical source URLs, license notices, and legitimate third-party authorship. Editing public content later does not retract prior exposure.

This review supplements repository checks. If an exposure occurs, revoke or rotate the affected credential first, remove the exposed material, and report only redacted evidence. Coordinate history rewriting separately with the owner.
